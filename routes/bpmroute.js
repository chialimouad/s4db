// routes/bpmroute.js
class WebSocketRoutes {
    constructor(websocketController) {
      this.websocketController = websocketController;
    }
  
    setupWebSocketRoutes(wss) {
      // WebSocket routes setup
    }
  }
  
  module.exports = WebSocketRoutes; // Make sure to export WebSocketRoutes
  